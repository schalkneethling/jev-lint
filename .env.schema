# @plugin(@varlock/1password-plugin)
# One 1Password approval per working day instead of one per command. The cached value is encrypted
# with Varlock's local encryption (see `varlock cache status` for its location). Clear it with
# `varlock cache clear --plugin 1password --yes`.
# @initOp(allowAppAuth=true, account=my, cacheTtl=8h)
# ---

# TypeSafe API key, read by @typesafe-ai/sdk
# @sensitive @required
TYPESAFE_API_KEY=op(op://dev/nocajwa573emc6ko6sv5bkn6y4/TYPESAFE_API_KEY)
